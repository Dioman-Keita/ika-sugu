# Connect to Supabase via connection pooling
DATABASE_URL="your-database-url"

# Direct connection to the database. Used for migrations
DIRECT_URL="your-database-direct-url-for-supabase"

# Environment
NODE_ENV="development"